{
  "name": "katas",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bunx moon run :test",
    "typecheck": "bunx moon run :typecheck",
    "lint": "bunx moon run :lint",
    "format": "bunx moon run :format"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@moonrepo/cli": "2.4.6",
    "@types/bun": "latest",
    "typescript": "7.0.2"
  }
}
