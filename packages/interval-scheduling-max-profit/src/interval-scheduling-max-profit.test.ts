import { describe, expect, test } from "bun:test";
import { intervalSchedulingMaxProfit } from "./interval-scheduling-max-profit";

describe("Interval Scheduling Max Profit", () => {
  test("solves the kata", () => {
    // TODO: replace with the real expected result once the kata is implemented.
    expect(intervalSchedulingMaxProfit()).toBe(undefined);
  });
});
