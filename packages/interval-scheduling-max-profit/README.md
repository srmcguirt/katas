# Interval Scheduling Max Profit

Select the combination of overlapping time-priced rental requests that maximizes total profit

Source: https://codingdojo.org/kata/Lags/

> Originally published as "Lags" on Coding Dojo.

## TDD loop

1. **RED** — run the tests. `src/interval-scheduling-max-profit.ts` throws `"Not implemented"`, so the test in `src/interval-scheduling-max-profit.test.ts` fails.
2. **GREEN** — implement just enough in `src/interval-scheduling-max-profit.ts` to make the test pass.
3. **REFACTOR** — clean up the implementation while keeping the test green. Add more test cases as needed.

Run tests for this package:

```
bun test
```

Or via moon:

```
moon run interval-scheduling-max-profit:test
```
