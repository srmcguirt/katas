import { describe, expect, test } from "bun:test";
import { movieRentalStatement } from "./movie-rental-statement";

describe("Movie Rental Statement", () => {
  test("solves the kata", () => {
    // TODO: replace with the real expected result once the kata is implemented.
    expect(movieRentalStatement()).toBe(undefined);
  });
});
