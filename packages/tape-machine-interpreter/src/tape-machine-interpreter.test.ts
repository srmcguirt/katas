import { describe, expect, test } from "bun:test";
import { tapeMachineInterpreter } from "./tape-machine-interpreter";

describe("Tape Machine Interpreter", () => {
  test("solves the kata", () => {
    // TODO: replace with the real expected result once the kata is implemented.
    expect(tapeMachineInterpreter()).toBe(undefined);
  });
});
