import { describe, expect, test } from "bun:test";
import { sudokuConcurrentResolver } from "./sudoku-concurrent-resolver";

describe("Sudoku the Concurrent Resolver", () => {
  test("solves the kata", () => {
    // TODO: replace with the real expected result once the kata is implemented.
    expect(sudokuConcurrentResolver()).toBe(undefined);
  });
});
