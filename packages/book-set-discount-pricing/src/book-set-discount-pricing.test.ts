import { describe, expect, test } from "bun:test";
import { bookSetDiscountPricing } from "./book-set-discount-pricing";

describe("Book Set Discount Pricing", () => {
  test("solves the kata", () => {
    // TODO: replace with the real expected result once the kata is implemented.
    expect(bookSetDiscountPricing()).toBe(undefined);
  });
});
